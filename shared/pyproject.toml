[project]
name = "shared-protocol"
version = "0.1.0"
description = "Shared protocol and crypto utils"
requires-python = ">=3.11"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
