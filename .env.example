# Server security and UI auth
SERVER_PSK=changeme
UI_USER=admin
UI_PASSWORD=admin
JWT_SECRET=devsecret
ALLOWED_HOSTS=example.com,localhost
# UI host binding for Vite
HOST=0.0.0.0

# Agent config path (override per host as needed)
# AGENT_CONFIG=/etc/orchestrator-agent/config.yaml
